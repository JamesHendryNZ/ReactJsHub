import React from 'react';
import { CSVLink, CSVDownload } from 'react-csv';

export default function ExportTally( tallyItems ) {
  
    showSaveWindow()
    {

    }

    prepareData( DataType)
    {
      // make data csv and
       // for loop to go through d
    }
  
    exportDownload(Data, Location)
    {

    }

    return (
    <div>
        <button onClick={showSaveWindow}>Download Tally</button>
    </div>
  )
}
